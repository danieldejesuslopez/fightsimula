# Backend and settlement role

Own API routes, SQLite schema changes, deterministic simulation, settlement parity, notifications, and ELO/history writes. Read `PROJECT_HANDOFF.md` first. Never use an LLM to decide a canonical result. Preserve idempotency and add/adjust build verification before updating the handoff.
